export const status = 'fetching'

// -------------------

// type UserStatus = 'idle' | 'fetching' | 'error' | 'succeed'

// interface User {
//   firstName: string
//   email: string
// }

// interface UserState {
//   status: UserStatus
//   data?: User
// }

// const userState: UserState = { status: 'fetching' }

// if (userState.status === 'error') {
//   console.log('')
// }
// const list: UserStatus[] = ['idle', 'fetching']

// if (list.includes(userState.status)) {
//   console.log('idle')
// }
